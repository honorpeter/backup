环：
    环与序列非常相似 它包含n个值    分辨关联到整数索引0 到N-1(当N为正值)
    其中的值都是指针    环中的值也可以用所以访问

    不同与序列的是  值可以添加到环中任意位置 而环中的任何值可以被删除
    此外环中的值还可重新编号 将环左 旋 会将每个值的索引减1 并对 环的长度
    取摸 将环右旋 则将每个索引值加1 并对换的长度取摸    


    接口：
        function/ring/







