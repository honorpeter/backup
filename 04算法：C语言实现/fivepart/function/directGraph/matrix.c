/*
 * 计算矩阵乘法；
 *  C = A*B
 */
for(s = 0; s < V; s++){
    for (t = 0; t < V; t++) {
        for(i = 0; C[s][t] = 0; i < V; i++){
            if(A[s][i] && B[i][i] )
                C[s][t] = 1;
        }
    }
}

/*
 * 更简单的算法
 * 计算稠密有向图的传递闭包的首选
 * 和上面的区别在于 for循环的顺序 这一点很重要
 */
for (i = 0; i < V; i++) {
    for (s = 0; s < V; s++) {
        for(t = 0; t <V ; t++){
            if(A[s][i] && A[i][t])
                A[s][t] =1;
        }
    }
}








