异常:
        stop("Could not open the file: ", filename)
        warning("File does not exist: ", filename)
    Catching Errors:
            tryCatch(expression, handler1, handler2, ..., finally=finalexpr)
            res <- try({x <- 1}, silent=TRUE)
环境变量:
        look the R in a Hntshell

