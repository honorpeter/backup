makefile语法  会学校自己写吧

有自动生成makefile工具 outmake
    outoscan
    outoconfige  ....可以生成 很多文件 就可以生成网上有的下载包了

    make dist变可以生成 网上可下载的一个文件  可以放在网站上 
            生成tar的文件

    ./configure  执行  他会检查 该程序需要的类库  看是否存在 
                还会生成 makefile文件
    make            生成可执行文件
    make install    移动文件  创建一个链接到/etc ...下面
        make uninstall 便可卸掉这个软件   删去这个链接

    make dist变可以生成 网上可下载的一个文件

