引用：
    声明函数传参引用：
        void hh(int & a){
            在这里可以直接用a 便可以需改外边的值
            指针的话 要加*a
        }
    传值：
        hh(b);

指针：
        void hh(int *a)
        {
            *a
        }
    传值：
        hh(&b);









