exit the program:
    exit(0) no error
    exit(1) something error

execfile( filename[, globals[, locals]])

传递参数:
        import sys
        sys.argv    sys.argc

@property 可以将python定义的函数“当做”属性访问








