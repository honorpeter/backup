原则：
    1 kiss  keep it simple and stupid
        函数简单
       




控制结构可以简化代码：
        if
        else if 。。。。。
    向这样的结构 一般可以简化代码：
    因为计算机是按顺序执行的
    所以：
        < 12
        < 56        这样可以简化

数据结构也可以简化代码：
    

一定要有错误处理：
    assert
    错误处理

保存数据结构：
    把数据结构和他们的操作放在在一起 封装起来
        可以使用强制类型转换
        。。。。


定义接口：
    接口分类：
        共享数据或变量名
        call-in functions
        callback functions
        同步调用接口
        异步调用接口
            信号量 消息队列等实现方式
    接口定义 反应了你的程序的耦合程度
